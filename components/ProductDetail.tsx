'use client';
import {useMemo,useState} from 'react';import type{Locale}from '@/lib/i18n';import{dict}from '@/lib/i18n';import type{Product}from '@/lib/products';import{useCart}from './CartContext';
export default function ProductDetail({product,locale}:{product:Product;locale:Locale}){const t=dict[locale],cart=useCart();const colors=product.options.find(o=>o.name==='Colour')?.values||[];const sizes=product.options.find(o=>o.name==='Size')?.values||[];const[color,setColor]=useState(colors[0]);const[size,setSize]=useState(sizes[0]);const variant=useMemo(()=>product.variants.find(v=>v.selectedOptions.some(o=>o.name==='Colour'&&o.value===color)&&v.selectedOptions.some(o=>o.name==='Size'&&o.value===size)),[product,color,size]);const stock=!variant||!variant.availableForSale?t.soldOut:variant.quantityAvailable<=3?t.lowStock:t.inStock;return <div className="container pdp"><div className="gallery">{product.images.map((src,i)=><img key={i} src={src} alt={`${product.title[locale]} ${i+1}`}/>)}</div><aside className="pdp-info"><p className="eyebrow">FLOALIVING</p><h1 style={{fontSize:38,fontWeight:400}}>{product.title[locale]}</h1><p>€{variant?.price.amount||product.variants[0].price.amount}</p><div className="option"><div className="option-label">{t.colour}</div><div className="chips">{colors.map(c=><button className={'chip '+(c===color?'active':'')} onClick={()=>setColor(c)} key={c}>{c}</button>)}</div></div><div className="option"><div className="option-label">{t.size}</div><div className="chips">{sizes.map(s=>{const v=product.variants.find(v=>v.selectedOptions.some(o=>o.name==='Colour'&&o.value===color)&&v.selectedOptions.some(o=>o.name==='Size'&&o.value===s));return <button disabled={!v?.availableForSale} className={'chip '+(s===size?'active':'')} onClick={()=>setSize(s)} key={s}>{s}</button>})}</div></div><p className="muted">{stock}</p><button className="add" disabled={!variant?.availableForSale} onClick={()=>variant&&cart.add(product,variant)}>{t.add}</button><p style={{fontSize:12}} className="muted">SKU: {variant?.sku}</p>{[[t.details,product.description[locale]],[t.fit,product.metafields.fit],[t.care,product.metafields.material+' · '+product.metafields.care],[t.delivery,'Free NL delivery over €150. Returns policy will be connected to Shopify operations.']].map(([a,b])=><details className="accord" key={a}><summary>{a}</summary><p className="muted">{b}</p></details>)}</aside></div>}
