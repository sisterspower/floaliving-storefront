import ProductCard from '@/components/ProductCard';import{localeFrom,dict}from'@/lib/i18n';import{products}from'@/lib/products';
export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale:raw}=await params;const locale=localeFrom(raw),t=dict[locale];return <main className="container"><header className="collection-head"><p className="eyebrow">FLOALIVING</p><h1 className="section-title">{t.robes}</h1></header><div className="collection-meta"><span>{products.filter(p=>p.category==='robes').length} products</span><span>FILTER + &nbsp;&nbsp; SORT +</span></div><div className="grid">{products.filter(p=>p.category==='robes').map(p=><ProductCard key={p.id} product={p} locale={locale}/>)}</div><div style={{height:90}}/></main>}
